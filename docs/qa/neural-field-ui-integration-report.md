# Neural Field UI integration report

1. **Repository and active branch:** `/Users/malmeida/Documents/Development/camera-harness-neural-field-integration`, `feature/sensefield-neural-field-pro`.
2. **Process serving port 4177:** the active launcher was started from the integrated worktree above and remains available for operator review at `http://localhost:4177/`.
3. **Root cause:** port 4177 served `checkpoint/sensefield-stable-single-voice-2026-07-11` at `5beec367`, which has no Neural Field primary UI. The issue was the wrong worktree/branch, not a cached bundle.
4. **Neural Field entry:** the active integration exposes a first-class `[ Ask ] [ Watch ] [ Neural Field ]` selector and compact Neural Field landing panel.
5. **AirScript production status:** the canonical lifecycle mounts the real hand geometry → pinch → trajectory → classifier → sanitized renderer-event path. No response label or final UI success is injected.
6. **Spatial Lasso production status:** closed-lasso validation commits one fresh spatial request, then production grounding and relation contracts drive render events. Ordinary drawing makes no cloud request, and tracking refresh is explicit rather than continuous.
7. **Hand worker integration:** the canonical lifecycle owns one local gesture worker; the duplicate presentation-owned worker path is disabled.
8. **Renderer integration:** the lifecycle owns one visible renderer and releases it on tool switch or exit. Raw landmarks are not retained by renderer, replay or persisted state.
9. **Object-grounding integration:** fresh scene evidence is normalized into candidate bounds. Missing, unsupported and ambiguous evidence remains unresolved instead of producing a claim; manual confirmation remains required.
10. **Relation integration:** two grounded candidates can resolve a supported relation and connector. A fresh explicit tracking result updates the relation or clears stale anchors on loss.
11. **Replay/privacy status:** replay is off by default, requires explicit consent, is bounded to 30 seconds, remains local, stores sanitized events only, and supports explicit discard/save. Exiting or switching stops replay; object URLs are revoked; no microphone, raw media, upload or hidden recording is used.
12. **Ask restart result:** deterministic browser and lifecycle flows passed; retained artifact: `docs/qa/screenshots/neural-field-ui/09-exit-ask.jpg`.
13. **Watch restart result:** deterministic browser and lifecycle flows passed; retained artifact: `docs/qa/screenshots/neural-field-ui/10-exit-watch.jpg`.
14. **Responsive result:** desktop and 390 px mobile layouts keep mode controls, camera, status, actions and manual-confirmation disclosure contained without page-level horizontal overflow; retained mobile artifact: `docs/qa/screenshots/neural-field-ui/11-mobile-layout.jpg`.
15. **Validation results:** `npm run sentinel:neural-field` passed with `AVAILABLE MODULAR_PRODUCTION_RUNTIME`. Measured: hand inference `11596.523 FPS`; event latency p50/p95 `0.068/0.194 ms`; renderer `121.279 FPS`; render time p50/p95 `0.1/0.2 ms`; pinch-to-line `0.936 ms`; lasso completion `2.746 ms`; grounding `0.348 ms`; relation render `0.124 ms`; memory growth `14,362,064 bytes`; worker queue depth `1`; renderer resources `1`; semantic requests `0`. All exact named baseline commands and the sentinel's nested regression copies passed, including `REAL_SPATIAL_MODEL_PASS`, browser lifecycle proofs, voice regressions, typecheck and `git diff --check`.
16. **Screenshot paths:** retained browser artifacts are `01-*` through `11-*` under `docs/qa/screenshots/neural-field-ui/`, covering primary entry, landing, AirScript live/stable recognition, active/grounded/relation/lost Spatial Lasso states, exits to Ask/Watch and mobile containment. Inputs are safe deterministic test-only inputs; screenshots do not claim physical-camera proof.
17. **Remaining limitations:** physical acceptance with a real camera and real hand remains manual. Production object and relation grounding also require a fresh available spatial-service result; unavailable or ambiguous evidence is reported honestly.
18. **Honest verdict:** automated integration proof, baseline regressions, live port verification and retained screenshots are complete without autonomous-vision, production-readiness or physical-camera claims.
